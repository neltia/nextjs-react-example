{
  // TypeScript 설정 파일
  "compilerOptions": {
    // 출력/소스 디렉터리
    "outDir": "./dist", // 컴파일된 JS 파일이 저장될 디렉터리
    // 출력할 JS 버전
    "target": "ES2020",
    // 모듈 시스템
    "module": "ESNext",
    // 모듈 해석 방식: Node.js 호환
    "moduleResolution": "node",
    // .js 확장자 없이 import했을 때, 컴파일 시 .js로 매핑하도록 허용
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    // type 엄격 검사 설정
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    // 라이브러리 옵션: Node.js 환경 + ES2020 전역 API
    "lib": ["ES2020", "DOM"],

    // 불필요한 유형 검사 방지 (서드파티 모듈 사용 시)
    "skipLibCheck": true

    // JSX 사용 여부 (React/Next.js 환경에서 필요 시)
    // "jsx": "react-jsx",
  },

  // 컴파일할 파일 및 디렉터리 지정
  "include": [
    "01_js_ts/**/*.ts", // TypeScript 파일 포함
  ]
}
